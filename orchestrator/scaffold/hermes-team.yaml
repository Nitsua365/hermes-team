# Hermes Team configuration
# All fields are optional — defaults shown as comments.

# data_dir: ~/.hermes-team          # persistent volume for all profiles + kanban board
# orchestrator_name: hermes-orchestrator
# orchestrator_port: 8642
# image: nousresearch/hermes-agent:latest
